<h4>Levendefødte - til målgruppeinformasjon</h4>
<p>Data om levendefødte ble sist importert for år <strong>{{ levendefodte.getLatestYearUpdated() }}</strong>.</p>
{% if levendefodte.getMissingYears()|length > 0 %}
	<div class="alert alert-warning">
		<strong>OBS! Data mangler for årene {% for year in levendefodte.getMissingYears() %}{{ year }}{{ loop.last ? '' : ', ' }}{% endfor %}</strong>
	</div>
{% endif %}
<form class="form" method="POST">
	<div class="form-group">
		<label for="year">Importer data for år</label>
		<input type="hidden" name="postform" value="levendefodte_update">
		<select name="year" id="year" class="form-control">
			{% for year in now|date("Y")..2000 %}
				<option value="{{ year }}">{{ year }}</option>
			{% endfor %}
		</select>
	</div>
	<div class="form-group">
		<button type="submit" class="form-control btn btn-success">Importer data fra SSB</button><br>
		<span class="" style="font-style: italic;">OBS: Dette kan ta noe tid!</span>
	</div>
</form>
{% if log is defined %}
	<ol class="list-group">
		{% for entry in log %}
			<li class="list-group-item text-{{ entry.success ? 'success' : 'danger' }}">
				Kommune {{ entry.id }}, antall {{ entry.antall }}. {{ entry.message }}
			</li>
		{% endfor %}
	</ol>
{% endif %}