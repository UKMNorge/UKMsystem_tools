<style>
    .mt-1 {
        margin-top: 0.25em;
    }
    .mb-0 {
        margin-bottom: 0;
    }
</style>
<h2>Administratorer</h2>

{% include "wp_modul.flashBag.html.twig" %}

<div class="container-fluid">
    {% for fylke in fylker %}
        <div class="row">
            <h4 class="mb-0" style="text-transform: uppercase">
                {{ fylke.getNavn() }}
            </h4>
            <ol class="list-group">
                {% for admin in fylke.getAdministratorer().getAll() %}
                    <li class="list-group-item">
                        <div class="float-right pull-right">
                            <a href="?page={{ GET('page') }}&removeAdmin={{ admin.getId() }}&fylke={{ fylke.getId() }}">
                                <span class="dashicons dashicons-trash"></span>
                            </a>
                        </div>
                        <b>{{ admin.getUser().getNavn() }}</b>
                        {{ admin.getUser().getPhone() }}
                    </li>
                {% else %}
                    <li class="list-group-item">
                        Ingen administratorer er lagt til enda.
                    </li>
                {% endfor %}
                <p class="mt-1">
                    <a href="?page={{ GET('page') }}&action=add&fylke={{ fylke.getId() }}">
                        Legg til administrator
                    </a>
                </p>
            </ol>

        </div>
    {% else %}
        <div class="alert alert-danger">
            En system-feil har oppstått. Fant ikke fylker
        </div>
    {% endfor %}
</div>
